CREATE TABLE obras.BARRIO (id BIGINT NOT NULL, EMAILADDRESS VARCHAR(100), FIRSTNAME VARCHAR(40), LASTNAME VARCHAR(40) NOT NULL, NOTES VARCHAR(4000), PHONENUMBER VARCHAR(30), version BIGINT NOT NULL, PRIMARY KEY (id))
CREATE TABLE obras.OBRA (id BIGINT NOT NULL, name VARCHAR(40) NOT NULL, version BIGINT NOT NULL, owner_id BIGINT, PRIMARY KEY (id))
ALTER TABLE obras.BARRIO ADD CONSTRAINT Barrio__lastName__UNQ UNIQUE (LASTNAME)
ALTER TABLE obras.OBRA ADD CONSTRAINT Barrio_name__UNQ UNIQUE (owner_id, name)
ALTER TABLE obras.OBRA ADD CONSTRAINT FK_OBRA_owner_id FOREIGN KEY (owner_id) REFERENCES obras.BARRIO (id)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT NUMERIC(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
